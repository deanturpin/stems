# Build artifacts
build/
*.o
*.a
*.so
*.dylib
stems

# Models (too large for git)
models/*.onnx
models/*.onnx.data
models/*.ort
models/*.ort.data
models/*.pt

# Audio files (test files only, except specific examples)
*.wav
*.flac
*.aiff

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Compile commands
compile_commands.json
